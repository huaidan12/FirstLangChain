# Vercel 部署用的精简依赖：只保留 bookkeeper 需要的包
# 完整依赖（含 coder：LangGraph + Milvus Lite）见 requirements-local.txt
# 换到 Render / Railway / 自建长驻环境时，请用 requirements-local.txt 安装

fastapi>=0.110.0
pydantic>=2.6.0

# LangChain 生态
langchain-core>=0.3.0,<0.4.0
langchain-openai>=0.3.0,<0.4.0
